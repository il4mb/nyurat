import { MessageStatusProps } from '@/internal/types'

type Props = {
    status: MessageStatusProps
}

export default function MessageStatus({ status }: Props) {
    return (
        <div>MessageStatus</div>
    )
}